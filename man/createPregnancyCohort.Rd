% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createPregnancyCohort.R
\name{createPregnancyCohort}
\alias{createPregnancyCohort}
\title{createPregnancyCohort}
\usage{
createPregnancyCohort(
  cdm,
  petTable,
  petSchema,
  keepExtensionTable = TRUE,
  cohortDefinitionID = 101,
  minGestationalDuration = 0,
  maxGestationalDuration = 308,
  minAge = 12,
  maxAge = 55,
  startDate = NULL,
  endDate = NULL,
  sex = "Female",
  outputDir,
  .softValidation = FALSE
)
}
\arguments{
\item{cdm}{(\code{cdm_reference}) Created with i.e. \code{CDMConnector::cdmFromCon}.}

\item{petTable}{(\code{character(1)}) Name of the Pregnancy Extension Table.}

\item{petSchema}{(\code{character(1)})  Name of the schema where the Pregnancy Extension Table resides}

\item{keepExtensionTable}{(\code{logical(1)}: \code{TRUE}) Should the intermediate table between the petTable and pregnancy_cohort be kept, default = TRUE}

\item{cohortDefinitionID}{(\code{numeric(1)}: \code{101}) Cohort definition id to assign to newly created cohort}

\item{minGestationalDuration}{(\code{numeric(1)}: \code{NULL}) Minimum gestational duration to include.}

\item{maxGestationalDuration}{(\code{numeric(1)}: \code{308}) Maximum gestational duration to include.}

\item{minAge}{(\code{numeric(1)}: \code{12}) Minimum age to include.}

\item{maxAge}{(\code{numeric(1)}: \code{55}) Maximum age to include.}

\item{startDate}{(\code{Date(1)}: \code{NULL}) Earliest pregnancy start date to include, e.g. as.Date("2001-09-20", "\%Y-\%m-\%d")}

\item{endDate}{(\code{Date(1)}: \code{NULL}) Latest pregnancy end date to include, e.g as.Date("10/20/21", "\%m/\%d/\%y")}

\item{sex}{(\code{character(2)}: \code{"Female"}) Sexes to include. One of or both \code{c("Female", "Male")}.}

\item{outputDir}{(\code{path}) Path to output pregnancy_duplicate_map.csv to}

\item{.softValidation}{(\code{logical(1)}: \code{FALSE}) Should a softValidation be done? default = FALSE}
}
\value{
(\code{cdm_reference}) Returns the CDM with the added cohort table.
}
\description{
Creates the pregnancy cohort from a specified pregnancy extension table (PET) in a specified schema
}
\note{
A pregnancy of multiples will be recorded with one pregnancy record
\itemize{
\item Multiple pregnancies of the same pregnancy_id these will be collapsed to one record.
\item If a multiples pregnancy with different pregnancy_ids for a birthing parent is recognized, then this will be collapsed to one pregnancy record with the smallest pregnancy_id kept to represent it
}
}
